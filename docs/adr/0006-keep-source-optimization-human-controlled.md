# Keep source optimization human-controlled

Lead Outcomes produce Source Performance metrics and same-niche Source Recommendations, but recommendations never alter acquisition automatically. Administrators approve immutable Scraper Configuration versions, every Scrape Run records its version, rollback selects a prior version without rewriting history, and activation waits for the next nightly run unless an administrator separately starts a manual run.
