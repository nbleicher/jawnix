# Commit distribution at allocation

A Distribution Event becomes permanent when allocation and batch generation commit, before email delivery. It snapshots the Customer, Agency, delivered phone, title, state, and the Listing Observation or Lead Correction used; delivery retries reuse the same batch, delivery failure never returns its Leads to inventory, and later identity, membership, inventory, or delivery changes never rewrite history.
